const assert = require('assert');
const proxyquire = require('proxyquire').noCallThru();

describe('first', () => {

  it('should pass', () => {

    const main = proxyquire('.', {

    });

  })

})
